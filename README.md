# MarkupCLI++
A C++ project that allows for easy creation of *Markdown files* using a CLI, for **Linux**, **Windows**, and **macOS**.

## Flags

- --header
- --text
- --note
- --important
- --tip
- --image